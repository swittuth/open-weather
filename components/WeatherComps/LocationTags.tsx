import Tag from './Tag';

const LocationTags = () => {
  return (
    <div className='flex justify-center items-center border-2 border-red-300'>
      {/* display all the locations chosen for weather so far */}
      {/* should performing mapping */}
      <Tag />
    </div>
  );
};

export default LocationTags;
